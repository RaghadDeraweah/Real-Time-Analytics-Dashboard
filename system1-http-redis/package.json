{
  "name": "system1-http-redis",
  "version": "0.1.0",
  "description": "HTTP + Redis ingestion pipeline with stateless workers and WebSocket broadcaster.",
  "main": "src/server.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src src/server.js",
    "start:worker": "node src/worker.js",
    "start:balancer": "node src/load-balancer.js",
    "start:ws": "node src/websocket-server.js",
    "lint": "echo \"TODO: add linter\""
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ioredis": "^5.3.2",
    "pino": "^8.16.0",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "pino-pretty": "^10.2.0"
  }
}

